import React from 'react'
import {data} from "../App"

const InputPosts = async(props) => {

    return (
      <div className="app">
        <h1>{props.weather.title}</h1>

      </div>
    )

}

export default InputPosts